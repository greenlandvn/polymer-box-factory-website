const canvas = document.getElementById('pdfCanvas'); const context = canvas.getContext('2d'); const url = 'yourfile.pdf'; // Ensure this matches the name of your PDF file const totalPages = 29; // Adjust this to the total number of pages in your PDF let currentPage = 1; pdfjsLib.getDocument(url).promise.then(pdf => { function renderPage(pageNumber) { pdf.getPage(pageNumber).then(page => { const viewport = page.getViewport({ scale: 1.5 }); canvas.height = viewport.height; canvas.width = viewport.width; const renderContext = { canvasContext: context, viewport: viewport }; page.render(renderContext); }); } // Start rendering the first page renderPage(currentPage); // Flip every 5 seconds setInterval(() => { currentPage = (currentPage % totalPages) + 1; renderPage(currentPage); }, 5000); }).catch(error => { console.error('Error loading PDF:', error); });

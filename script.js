const canvas = document.getElementById('pdfCanvas'); const context = canvas.getContext('2d'); const url = 'yourfile.pdf'; // Your PDF in the repo const totalPages = 29; let currentPage = 1; const loadingTask = pdfjsLib.getDocument(url).promise; loadingTask.promise.then(pdf => { function renderPage(pageNumber) { pdf.getPage(pageNumber).then(page => { const viewport = page.getViewport({ scale: 1.5 }); canvas.height = viewport.height; canvas.width = viewport.width; const renderContext = { canvasContext: context, viewport: viewport }; page.render(renderContext); }); } // Start rendering renderPage(currentPage); // Flip every 5 seconds setInterval(() => { currentPage = currentPage % totalPages + 1; renderPage(currentPage); }, 5000); });
